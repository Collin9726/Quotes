import { DeleteQuoteDirective } from './delete-quote.directive';

describe('DeleteQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
